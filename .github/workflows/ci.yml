name: ci
on:
  push:
  pull_request:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m py_compile a2a_relay/*.py tests/*.py
      - run: python -m unittest discover -s tests -q
      - run: python -m a2a_relay --base /tmp/a2a init --agent lulu@kamac --agent zhiwei@known-blocks1
      - run: python -m a2a_relay --base /tmp/a2a send --from lulu@kamac --to zhiwei@known-blocks1 --type request --subject hi --body hello --needs-reply
      - run: python -m a2a_relay --base /tmp/a2a pending --agent zhiwei@known-blocks1
      - run: python -m a2a_relay --base /tmp/a2a poll --agent zhiwei@known-blocks1 --allow-from lulu@kamac --ack
      - run: python -m a2a_relay --base /tmp/a2a threads
