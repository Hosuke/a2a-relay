{
  "contacts": {
    "worker@example": {
      "id": "worker@example",
      "display_name": "worker",
      "aliases": ["worker"],
      "transport": "filesystem",
      "inbox": "/root/agent-mailbox/inbox/worker_example",
      "allow_from": [],
      "allowed_types": [
        "alert",
        "handoff",
        "heartbeat",
        "memory",
        "note",
        "reply",
        "request",
        "status"
      ],
      "trust_level": "trusted-filesystem",
      "notes": "example private contact",
      "safe_name": "worker_example"
    },
    "operator@example": {
      "id": "operator@example",
      "display_name": "operator",
      "aliases": ["operator"],
      "transport": "filesystem",
      "inbox": "/root/agent-mailbox/inbox/operator_example",
      "allow_from": ["worker@example"],
      "allowed_types": ["request", "reply", "status", "note"],
      "trust_level": "trusted-filesystem",
      "notes": "allow_from and allowed_types are metadata unless active policy enforces them",
      "safe_name": "operator_example"
    }
  }
}
